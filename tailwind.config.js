/**
 * Tailwind v4: design tokens and `@theme` live in `src/styles/index.css` + `src/theme/brand.css`.
 * This file keeps `content` paths explicit for tooling; colors are **not** duplicated here.
 */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
};
