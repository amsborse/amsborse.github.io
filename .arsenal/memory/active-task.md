# Active Task

## Status

Arsenal context engine initialized for `amsborse.github.io`.

## Current focus

_No active task — update this file when starting agent work._

## Context pointers

- Curated graph: `.cursor/knowledge-graph/graph.json`
- Auto graph: `.arsenal/knowledge-graph.json`
- Quality gates: `npm run validate` + `npm run test:e2e`

## After completing work

1. Update this file or run `npm run context:compact` (Arsenal CLI)
2. Run `npm run verify:kg` if routes/components changed
3. Run `npm run context:refresh` after large architectural changes
